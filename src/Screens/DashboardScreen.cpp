#include "Screen.h"
#include "Drawers.h"

class DashboardScreen: public Screen {
    void draw() override {
        // Implement drawing code for the dashboard screen
    }

    void update() override {
        // Implement update logic for the dashboard screen
    }
};